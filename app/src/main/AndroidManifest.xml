<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tiantian.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activity.wxarticle.ChaptersActivity" />
        <activity android:name=".activity.wxarticle.ArticleListActivity" />
        <activity android:name=".activity.wxarticle.ArticleActivity" />
        <activity android:name=".activity.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.webview.WebViewActivity" />
        <activity android:name=".activity.search.SearchActivity" />
        <activity android:name=".activity.photo.PhotoActivity" />
        <activity android:name=".activity.photo.AlbumActivity" />
        <activity android:name=".activity.tree.TreeActivity" />
        <activity android:name=".activity.SearchResultActivity" />
        <activity android:name=".activity.login.LoginActivity"></activity>
    </application>

</manifest>